# Copy to .env.local and fill in real values. .env.local is gitignored, never commit secrets.

# React Bits Pro license key, used by components.json to install blocks via the shadcn CLI.
REACTBITS_LICENSE_KEY=
